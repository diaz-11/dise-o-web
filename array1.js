const numbers = [1, 2, 3, 4, 5];
numbers[0] = 10;
numbers[2] = 30;
console.log(numbers); // [10, 2, 30, 4, 5]

// ¿Qué se imprimirá en la consola al ejecutar el siguiente código?
//se remplaza los valores iniciales con los nuevos valores, en este caso el valor 0 se remplaza
//por el valor 10, y el valor 2 se remplaza con el 30.
